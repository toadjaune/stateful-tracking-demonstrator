<!DOCTYPE html>
<html>
  <head>
    <title>Stateful tracking demo</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%#= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <% # We don't really need the basic rails js. Let's remove it for now. %>
  </head>

  <body>
    <header>
      <h1> <a href="/">Stateful tracking demo</a> </h1>
      <ul>
        <% if user_signed_in? %>
          <li> <%= current_user %> </li>
          <li> <%= button_to('Logout', destroy_user_session_path, method: :delete) %></li>
        <% else %>
          <li> <%= button_to('Login', new_user_session_path, method: :get) %> </li>
          <li> <%= button_to('Sign up', new_user_registration_path, method: :get) %> </li>
        <% end %>
      </ul>
    </header>
    <% if notice != nil %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if alert != nil %>
      <p class="alert"><%= alert %></p>
    <% end %>
    <div id="content">
      <%= yield %>
    </div>
  </body>
</html>
